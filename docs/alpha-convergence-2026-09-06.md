# Alpha 收敛记录

日期：2026-09-06。范围以 `05-ALPHA-PLAN.md` 为准；本文是执行证据记录，不修改产品要求或验收门槛。

## 结论

本轮工程阻断已修复：真实 Host 共思答案绑定、证据结构输出和评审依据解析均有回归证据。同一构建的干净恢复通过，40-case 混合评估完整执行：36 通过、4 失败（公开 28/32，私有 8/8）。四条失败均为公开案例使用了案例外合成 Twin 背景，语义门禁未通过。Alpha 尚未完成，不生成通过候选。全量记忆、写作共思、持续代办不扩展进本轮。

用户跳过个人原始行动／结果确认；该项保持 `skipped_by_user / unverified`，不再次索要确认，也不把未知来源资料、模型生成案例或合成测试当作真实学习证据。

## 已落实

- 评估绑定错误增加阶段及安全错误类别，保留原有精确请求、回答摘要、事务日志和原始证据校验。
- 私有评估运行器在绑定校验前保存本轮运行 ID 及请求／回答摘要，不在公开资产中记录正文。
- 证据输出 Schema 与运行时的基本依据要求一致：事实／推断必须有原始支持或明确未决来源限制；不补造依据。
- 非法断言细分为结构、ID、文本、类型、引用和未决来源字段错误，不放宽拒绝条件。
- 测试入口先构建运行产物，消除安装器测试对历史 `dist` 的隐式依赖。

本地验证：`npm test` 共 247 项，244 通过、3 跳过、0 失败；类型编译及 5 项 Schema 校验通过。此前独立 `npm run check` 和 `git diff --check` 通过。跳过项不是通过证据。

## 失败证据与解释边界

- 历史混合评估在首个公开案例报告 `evaluation_answer_binding_unavailable`，未形成 40-case 完整报告。
- 对该案例已保存产物的只读重放，在原运行权限下可通过；请求和回答摘要一致，客户端 ID 与内部运行 ID 也一致。ID 不一致的猜测已排除；原失败原因尚未证实，不能称为已修复。
- 新构建恢复第一次在重要开放状态探针出现 `unsupported_bundle_claim`；第二次在身份探针出现 `invalid_bundle_claim`。两者都是拒绝结果，不能沿用历史成功收据宣称新构建通过。
- 第三次恢复在身份探针明确返回 `invalid_bundle_claim_shape`，已缩小到断言对象字段结构；尚未记录到具体缺失／多余字段，不能推断为引用错误或答案摘要不匹配。源码快照 `28083c8a4d6272ceb1f2ced5735fce26567d1923`，产物 SHA-256 `74fce75cf51a9f11bcfcb7128bb79af9ffdcfc02928ce6df6d00dee71173c429`，模型 `google/gemini-3.1-pro-preview`，未生成通过收据。
- 已检查确切 Host 的 `LlmCompleteParams`：`runtime.llm.complete` 没有 JSON Schema 强制输出参数。初始诊断阶段未加修正；后续有界修正见下文。本轮未绕过宿主另接模型、关闭校验或以偶然成功替换失败记录。

以上为初始失败阶段记录；其后工程修复及同构建验证见下文。历史首例的瞬时绑定失败原因仍未证实，与已确认并修复的共思分支遗漏分别记录。

### 本轮工程修复后续

- 两次隔离响应捕获均为合法结构，未得到原先坏响应，不能宣称已重现其具体缺失字段。临时捕获代码已从验收源码移除，捕获产物只留在私有诊断位置。
- `stella-question-evidence/v5` 增加一次有反馈的模型结构修正，总计最多两次；完整契约校验保持不变，不由代码补字段、删除断言或生成兜底澄清。该版本尚未覆盖依据约束修正；未读引用、证据不足却给建议等失败始终不走该修正。
- 合成测试复现结构失败后验证修正成功、二次失败明确终止，以及取消和代际变化不会触发后续模型调用。安全诊断保留尝试类别及响应摘要。
- 源码 `4061a8a0ce852d18bfc1a39211743b9de2ff76f1`、产物 SHA-256 `e926a7e5fb0e90574b8c8f7cc8fb19417822347cbcd9ce53833898e6de47f133` 的干净恢复通过，3 次原生回答及连续性评审通过。其中一次真实触发 `invalid_bundle_claim_references → accepted`，其他两次首次通过；不表示每次运行都能通过，也不取消历史失败。
- 同一构建的 32+8 混合评估随后启动；没有完整结果前，原答案绑定问题仍未关闭。恢复仍只证明所选隔离源，包括合法空学习／开放状态，不证明真实实践学习。

混合评估随后在第二个公开案例的评审解析阶段停止：前两个案例均已完成原生回答与证据绑定，首例七项诊断通过。第二例原生评审将七条依据编码为“准确维度键 → 单元素字符串数组”，旧解析器误报缺少依据。

`stella.praxis-rubric/v4` 明确输出 Schema，并将完整七键、每键一个字符串（或单元素字符串数组）的依据无损转为带维度标签的七条理由。原评分及理由原文不变，空值、缺键、多余键、多元素数组和嵌套对象仍拒绝。对同一原生响应的只读重放由失败变为通过，模型调用为零、七项原评分全部保留；这不是重新评分，也不能把未执行的其余案例计为通过。

包含评审格式修复的 `8df7912f596862f01e3f38ad34a75cf71e19d907` 构建恢复在第三个探针出现 `bundle_claim_evidence_not_read`，按非结构错误直接阻断，未生成通过收据。

为避免模型拼写引用 ID／哈希，`stella-question-evidence/v6` 将模型接口改为本轮已读原件的 `E1` 等短编号。模型仍做语义选择，代码只将选中编号解析为确切已读版本；持久 EvidenceBundle 格式不变。输出 Schema 列出可选编号，空集合不可选择；未知编号、重复选择和引用对象均被拒绝。非空原始证据 fixture 已证明版本绑定和原件回读不变。

中间构建源码为 `ac3f08dab40a15171f6e52f0c282c86557b62bfe`，产物 SHA-256 `f2cafd1494dadeab455728d0e04936bc06ef585bf402183f15ee9873b7f25091`；其干净恢复及连续性评审通过，三个证据评估均首次通过。同一构建随后启动混合评估，不继承前一构建的部分结果。

该次混合评估完成并通过前三例，第四例再次出现绑定失败，诊断为 `binding_record / ENOENT`。原生 `context.compiled` 明确记录 `responseKind=collaboration`；该副本根本没有证据事务目录。代码仅为 answer／clarification／action_advice 安排证据事务，遗漏 collaboration，故不是此次读文件过早或运行 ID 不一致。

修复将 collaboration 纳入同一证据事务及 critical 同步路径，并在 managed 问题准备结束时拒绝缺少持久化处理的分支。没有把共思强制改成行动建议，也没有创建虚构 Episode。主插件准备回归及 Git 提交、pointer 失败恢复、干净克隆和精确答案绑定回归均通过。这里只补齐共思回答的 EvidenceBundle／事务收据，不宣称完整 OngoingWork、学习或跨会话共思管理已实现。

包含共思绑定修复的 `b09571e91d6cf2add9dd45322d0fa65ded48adc7` 构建（产物 SHA-256 `79a9e6d3d89acee79d1e09cf7e013860332d8ce63c1177ea3b7dbc16c226ad17`）在第三个恢复探针因 `unsupported_bundle_claim` 阻断，未启动混合评估。

`stella-question-evidence/v7` 将 Schema 的“support 或 unresolved 非空”约束纳入同一最多一次的模型补正。该错误表示缺少依据或来源限制字段，不是已经完成事实真伪核验；模型必须重新给出完整合法评估，代码不生成依据。回归验证没有原件时只能保留明确来源限制，未知证据编号仍立即失败。临时响应捕获文件及捕获代码已清理；历史失败日志保留。

当前构建 `43bf05a42a4600e74df0ea9cef3bb2d32f58d228`（产物 SHA-256 `eef727064206a8f7ca93cc9b7dbc5aa3fcf4d0736463ceac0a1ad89034e345a1`）干净恢复通过：3 个原生探针、精确答案绑定和连续性评审完成，三个证据评估均首次通过。同包 32+8 混合评估已全部完成，未发生绑定缺失或评审解析中断；退出码 1 表示四条语义诊断失败，而非评估没有执行完。

该构建混合评估第四例已经完成；原生编译上下文确认仍为 `collaboration / sufficient`，证据绑定已产生并回读通过，七维诊断通过。这为已确认的共思分支遗漏提供原生修复证据，不追溯证明历史首例瞬时失败的原因。

第九例 `help-01` 工程链路正常，但 `personalContextUse=false`：回答将公开测试仓库中的合成 Twin 假设用于未声明该背景的案例人物。已核对来源是测试 fixture 的“Prefers reversible experiments”，不是私有资料。保留该诊断失败；不修改案例或评审门槛，不将工程运行成功等同于全部语义通过。

最终失败公开案例为 `help-01`、`workplace-03`、`conflict-01`、`conflict-02`，均仅 `personalContextUse=false`；其他六个维度及 8 个私有案例均通过。这提示下一优先修复是公开案例人物与通用测试 Twin 的上下文归属边界，不能通过放宽 rubric 或把测试背景追加成案例事实来消除失败。

最终恢复收据与评估报告的 Core SHA、私有数据源 SHA、Host 版本、安装包 SHA-256 一致，并重新计算了安装包摘要。公开源为独立合成仓库 `fb5f73879ff638aeedd08be6ec790ed6852d3850`；私有隔离源为 `f83d5628128f786226fa819b7bab40faa543d39e`。主工作树仍未提交；该隔离源码副本干净、无远端。完整结果不等于真实学习验收，也不满足全部候选出口。

## Alpha 出口状态

| 出口 | 当前结论 |
| --- | --- |
| A-01–A-07 | 有实现和本地回归；新版同一构建的完整 Exact Host 证据未齐 |
| A-08–A-09 | 真实行动到学习、后续使用未验收；用户确认步骤已跳过 |
| A-10、A-13 | 有合成持久化／故障回归；不能代替最终私有闭环证据 |
| A-11–A-12 | 当前构建隔离恢复通过合法空集合；非空重要状态不以空集合代替 |
| A-14、混合评估 | 当前恢复与完整 40-case 报告的构建字段一致；语义门禁失败，私有闭环等完整候选证据仍未齐 |

还需完成 Alpha 路径涉及的 Episode v2 建议修订、匹配的私有验收运行器及剩余 Host 故障验证。不得将当前的显式未支持分支直接移除后称为实现。

没有改动原始个人仓库、激活生产实例或执行发布；测试提交仅存在无远端的隔离源码副本。
